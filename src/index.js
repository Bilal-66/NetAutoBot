import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import "./styles/global.css"; // Fichier global pour les animations et transitions

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  /* ✅ Vérification des projets et affichage */
  /* ✅ Vérification des projets et affichage */
  /* ✅ Vérification des projets et affichage */
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
